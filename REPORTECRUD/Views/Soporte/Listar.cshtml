@model IEnumerable<REPORTECRUD.Models.SoporteTecnico>
@{
    ViewData["Title"] = "Listar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Listar</h1>

<p>
    <a asp-action="Guardar" asp-controller="Soporte" class="btn btn-primary btn-sm">Nuevo Soporte Tecnico</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdSoporteTec)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mensaje)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdSoporteTec)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Mensaje)
                </td>

                <td>
                    <a asp-action="Editar" asp-controller="Soporte" asp-route-IdSoporteTec="@item.IdSoporteTec">Editar</a>
                    <a asp-action="Eliminar" asp-controller="Soporte" asp-route-IdSoporteTec="@item.IdSoporteTec">Eliminar</a>
                    @*@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
                </td>
            </tr>
        }
    </tbody>
</table>